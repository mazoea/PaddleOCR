# Core dependencies
paddlepaddle==3.2.0
paddleocr==3.2.0
opencv-python-headless==4.8.1.78  # ← Downgrade from 4.12
numpy==1.26.4  # ← Pin to stable 1.x version

# AWS SDK (for S3 integration)
boto3==1.40.42

# Additional dependencies that may be needed
Pillow==11.3.0
shapely==2.0.6  # Has pre-built wheels for Python 3.12
pyclipper==1.3.0.post6